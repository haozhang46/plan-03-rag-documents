apiVersion: v1
kind: Service
metadata:
  name: agent-flow-backend
  namespace: agent-flow
  labels:
    app.kubernetes.io/name: agent-flow-backend
    app.kubernetes.io/part-of: agent-flow
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: agent-flow-backend
  ports:
    - name: http
      port: 80
      targetPort: http
