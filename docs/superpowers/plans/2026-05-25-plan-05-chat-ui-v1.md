# Plan 05: V1 Chat UI

> **For agentic workers:** REQUIRED SUB-SKILL: subagent-driven-development or executing-plans.

**Goal:** 最小可用对话界面：多 `thread_id` 会话、流式 SSE、文档上传、展示 citations。

**Architecture:** Next.js App Router 调用 `NEXT_PUBLIC_API_URL`；或文档说明 Open WebUI 自定义 OpenAI-compatible endpoint 指向我们的 API（二选一，默认 Next.js）。

**Tech Stack:** Next.js 15, React 19, TypeScript, fetch EventSource polyfill

**Depends on:** Plan 01–04

---

## 方案选择（实施前确认 1 项）

- **A（推荐）** 自研 `web/` — 完全可控，PRD 对标 Claude UI
- **B** Open WebUI — 仅写 `docs/open-webui-setup.md`，不建 `web/`

以下 Task 按 **方案 A** 编写。

---

## File Map

```text
web/
├── package.json
├── app/
│   ├── layout.tsx
│   ├── page.tsx              # 会话列表 + 聊天区
│   └── api/                  # 可选 BFF 代理
├── components/
│   ├── ChatMessage.tsx
│   ├── ChatInput.tsx
│   └── DocumentUpload.tsx
└── lib/
    ├── api.ts                # SSE client
    └── threads.ts            # localStorage thread ids
```

---

### Task 1: Next.js 初始化

- [ ] **Step 1:** `npx create-next-app@latest web --ts --app --eslint`

- [ ] **Step 2:** `.env.local` — `NEXT_PUBLIC_API_URL=http://localhost:8000`

- [ ] **Step 3: Commit**

---

### Task 2: SSE Client

**Files:** `web/lib/api.ts`

- [ ] **Step 1: streamChat**

```typescript
export async function* streamChat(
  threadId: string,
  message: string,
  documentIds?: string[],
): AsyncGenerator<{ content?: string; citations?: string[]; done?: boolean }> {
  const res = await fetch(`${process.env.NEXT_PUBLIC_API_URL}/v1/chat`, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ thread_id: threadId, message, document_ids: documentIds }),
  });
  const reader = res.body!.getReader();
  const dec = new TextDecoder();
  let buf = "";
  while (true) {
    const { done, value } = await reader.read();
    if (done) break;
    buf += dec.decode(value);
    for (const line of buf.split("\n")) {
      if (line.startsWith("data: ")) {
        const payload = JSON.parse(line.slice(6));
        yield payload;
      }
    }
    buf = "";
  }
}
```

- [ ] **Step 2: Commit**

---

### Task 3: 聊天页 UI

**Files:** `web/app/page.tsx`, `web/components/ChatInput.tsx`

- [ ] **Step 1:** localStorage 维护 `threads: {id, title}[]`

- [ ] **Step 2:** 发送消息调用 `streamChat`，追加 assistant 气泡

- [ ] **Step 3:** Markdown 渲染（`react-markdown`）

- [ ] **Step 4: Commit**

---

### Task 4: 文档上传组件

**Files:** `web/components/DocumentUpload.tsx`

- [ ] **Step 1:** `POST /v1/documents` multipart，保存 `document_id` 到 state

- [ ] **Step 2:** chat 时传入 `document_ids`

- [ ] **Step 3: Commit**

---

### Task 5: Citations 展示

- [ ] **Step 1:** assistant 消息下方渲染 `citations` 为 tag 列表

- [ ] **Step 2: 端到端手动测试** — 上传 txt → 提问 → 见 citation

- [ ] **Step 3: Commit**

---

## V1 完成检查清单

- [ ] Plan 01–05 全部 commit
- [ ] README：启动顺序 `docker compose up` → `uvicorn` → `npm run dev`
